name: 🚀 CI/CD workflow

on:
  push:
    branches:
      - main
      - feat/*
      - chore/*
  pull_request:
    branches:
      - main

permissions:
  contents: write
  checks: write
  pull-requests: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    outputs:
      tests_passed: ${{ steps.test-status.outputs.tests_passed }}
    env:
      ConnectionStrings__openai: ${{ secrets.OPENAI_CONNECTION_STRING }}
      ApplicationInsights__ConnectionString: ${{ secrets.APPLICATIONINSIGHTS_CONNECTION_STRING }}
      APPLICATIONINSIGHTS_CONNECTION_STRING: ${{ secrets.APPLICATIONINSIGHTS_CONNECTION_STRING }}
      AZURE_CLIENT_ID: ${{ vars.AZURE_CLIENT_ID }}
      AZURE_TENANT_ID: ${{ vars.AZURE_TENANT_ID }}
      AZURE_SUBSCRIPTION_ID: ${{ vars.AZURE_SUBSCRIPTION_ID }}
      AZURE_ENV_NAME: ${{ vars.AZURE_ENV_NAME }}
      AZURE_LOCATION: ${{ vars.AZURE_LOCATION }}

    steps:
    - name: 🧑‍💻 Checkout Repository
      uses: actions/checkout@v4

    - name: 🔧 Set Up .NET Core SDK
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '9.x'

    - name: 🔐 Trust ASP.NET Core Developer Certificate
      run: dotnet dev-certs https --trust

    - name: 🛠️ Install azd
      uses: Azure/setup-azd@v2

    - name: 🛠️ Install trx2junit Tool
      if: github.event_name == 'pull_request'
      run: dotnet tool install --global trx2junit
      
    - name: 📦 Restore Dependencies
      run: dotnet restore

    - name: 🏗️ Build Project
      run: dotnet build --configuration Release
        
    - name: 🧪 Run All Tests
      id: run-tests
      run: |
        mkdir -p ./test-results
        all_tests_passed=true
        
        for proj in $(find test -name "*.csproj"); do
          proj_name=$(basename $proj .csproj)
          if ! dotnet test "$proj" --configuration Release --logger "trx;LogFileName=${proj_name}.trx" --results-directory ./test-results; then
            all_tests_passed=false
          fi
        done
        
        echo "all_tests_passed=${all_tests_passed}" >> $GITHUB_OUTPUT
      shell: bash

    - name: 📊 Set Test Status
      id: test-status
      run: |
        if [ "${{ steps.run-tests.outputs.all_tests_passed }}" == "true" ]; then
          echo "tests_passed=true" >> $GITHUB_OUTPUT
        else
          echo "tests_passed=false" >> $GITHUB_OUTPUT
          echo "❌ Some tests failed. Deployment will be skipped."
        fi

    - name: 🔄 Convert Test Results to JUnit Format
      if: github.event_name == 'pull_request'
      run: find ./test-results -name '*.trx' -exec trx2junit {} \;

    - name: 📑 Publish Test Results as PR Comment
      if: github.event_name == 'pull_request'
      uses: EnricoMi/publish-unit-test-result-action@v2
      with:
        files: '**/test-results/*.xml'
        github_token: ${{ secrets.GITHUB_TOKEN }}

    - name: ✅ Add Check Comments for Failed Code Lines
      if: github.event_name == 'pull_request'
      uses: mikepenz/action-junit-report@v3
      with:
        report_paths: '**/test-results/*.xml'
        github_token: ${{ secrets.GITHUB_TOKEN }}

    - name: 🚀 Log in with Azure (Federated Credentials)
      if: github.event_name == 'push' && github.ref == 'refs/heads/main' && steps.test-status.outputs.tests_passed == 'true'
      shell: pwsh
      run: |
        azd auth login `
          --client-id "$Env:AZURE_CLIENT_ID" `
          --federated-credential-provider "github" `
          --tenant-id "$Env:AZURE_TENANT_ID"

    - name: ⚙️ 디버그: Azure Developer CLI 버전 및 상태 확인
      if: github.event_name == 'push' && github.ref == 'refs/heads/main' && steps.test-status.outputs.tests_passed == 'true'
      shell: pwsh
      run: |
        Write-Host "🔍 azd 버전: $(azd version)"
        Write-Host "📂 현재 디렉토리: $(Get-Location)"
        Write-Host "📄 파일 목록:"
        Get-ChildItem -Force
        if (Test-Path azure.yaml) {
          Write-Host "✅ azure.yaml 파일 존재"
          Get-Content azure.yaml
        } else {
          Write-Host "❌ azure.yaml 파일 없음"
        }

    - name: 🚢 환경 변수 설정 및 인프라 준비
      if: github.event_name == 'push' && github.ref == 'refs/heads/main' && steps.test-status.outputs.tests_passed == 'true'
      shell: pwsh
      run: |
        # 리소스 그룹이 존재하지 않으면 생성
        $resourceGroup = "$Env:AZURE_ENV_NAME-rg"
        $location = "$Env:AZURE_LOCATION"
        
        Write-Host "🔍 리소스 그룹 확인: $resourceGroup, 위치: $location"
        
        # 리소스 그룹 확인 및 생성
        $rgExists = $(az group exists --name $resourceGroup)
        if ($rgExists -eq "false") {
          Write-Host "📦 리소스 그룹 생성 중..."
          az group create --name $resourceGroup --location $location
        } else {
          Write-Host "✅ 리소스 그룹 확인 완료"
        }
        
        # 필요한 환경 변수들을 설정
        Write-Host "🔧 환경 변수 설정 중..."
        
        # Application Insights 연결 문자열 확인 및 설정
        if ([string]::IsNullOrEmpty("$Env:ApplicationInsights__ConnectionString") -and [string]::IsNullOrEmpty("$Env:APPLICATIONINSIGHTS_CONNECTION_STRING")) {
          Write-Host "❌ Application Insights 연결 문자열이 설정되지 않았습니다" -ForegroundColor Red
        } else {
          Write-Host "✅ Application Insights 연결 문자열이 설정되었습니다" -ForegroundColor Green
        }

    - name: 🚢 어플리케이션 배포 및 환경 변수 설정
      if: github.event_name == 'push' && github.ref == 'refs/heads/main' && steps.test-status.outputs.tests_passed == 'true'
      shell: pwsh
      run: |
        # 현재 리소스 그룹 이름과 컨테이너 앱 이름 가져오기
        $resourceGroup = "$Env:AZURE_ENV_NAME-rg"
        $webApp = "webfrontend"
        $apiApp = "apiservice"
        
        Write-Host "🔍 리소스 그룹: $resourceGroup, 웹앱: $webApp, API앱: $apiApp"
        
        # 애플리케이션 배포 (간단한 예시)
        Write-Host "🚀 애플리케이션 배포 중..."
        
        # 환경 변수를 직접 설정
        Write-Host "🔧 컨테이너 앱 환경 변수 설정 중..."
        
        # 웹프론트엔드 앱 환경 변수 설정
        az containerapp update --name $webApp --resource-group $resourceGroup --set-env-vars "APPLICATIONINSIGHTS_CONNECTION_STRING=$Env:APPLICATIONINSIGHTS_CONNECTION_STRING" "ApplicationInsights__ConnectionString=$Env:ApplicationInsights__ConnectionString" "ConnectionStrings__openai=$Env:ConnectionStrings__openai"
        
        # API 서비스 앱 환경 변수 설정
        az containerapp update --name $apiApp --resource-group $resourceGroup --set-env-vars "APPLICATIONINSIGHTS_CONNECTION_STRING=$Env:APPLICATIONINSIGHTS_CONNECTION_STRING" "ApplicationInsights__ConnectionString=$Env:ApplicationInsights__ConnectionString" "ConnectionStrings__openai=$Env:ConnectionStrings__openai"
        
        Write-Host "✅ 환경 변수 설정 완료"
      env:
        # 배포 명령에서도 환경 변수 직접 전달
        APPLICATIONINSIGHTS_CONNECTION_STRING: ${{ secrets.APPLICATIONINSIGHTS_CONNECTION_STRING }}
        ApplicationInsights__ConnectionString: ${{ secrets.APPLICATIONINSIGHTS_CONNECTION_STRING }}
